# Handoff — EGOS Agenciação + Claude Code Setup

> **Session:** 2026-03-27 | **Ambiente:** VSCode Extension (claude-sonnet-4-6)
> **Contexto:** Continuação do handoff_2026-03-26.md — Agenciação + Config Claude Code
> **Próxima sessão:** Terminal Claude Code (claude --dangerously-skip-permissions ou padrão)

---

## 🎯 O QUE FOI REALIZADO HOJE

### 1. Distribuição de `.claude/` para todos os 10 repos leaf ✅

Todos os repos abaixo foram configurados com `.claude/` e commitados:

| Repo | Commit | Status |
|------|--------|--------|
| forja | `chore(.claude)` | ✅ |
| carteira-livre | `chore(.claude)` | ✅ |
| 852 | `chore(.claude)` | ✅ |
| br-acc | `chore(.claude)` | ✅ |
| smartbuscas | `chore(.claude)` | ✅ |
| INPI | `chore(.claude)` | ✅ |
| santiago | `chore(.claude)` | ✅ |
| policia | `chore(.claude)` | ✅ |
| egos-self | `chore(.claude)` | ✅ |
| commons | `chore(.claude)` | ✅ |

**Script usado:** `/home/enio/egos/.claude/batch-setup-all-repos.sh`

**Estrutura instalada em cada repo:**
```
.claude/
├── config/         # JSON copies: mcp-servers.json, permissions.json, memory.json, output-styles.json
├── hooks/          # Symlinks → ~/.egos/.claude/hooks/
├── commands/       # Symlinks → ~/.egos/.claude/commands/
└── manifest.json   # Metadata do repo
```

---

### 2. ETL Orchestrator Agent — EGOS-103 P0 ✅

**Commit:** `2db4154` em `egos-lab`

**Arquivos criados:**
- [`egos-lab/agents/agents/etl-orchestrator.ts`](../../egos-lab/agents/agents/etl-orchestrator.ts) — Agente de monitoramento (340 linhas)
- [`egos-lab/test-etl-orchestrator.ts`](../../egos-lab/test-etl-orchestrator.ts) — Testes de integração (todos passando)
- `egos-lab/agents/registry/agents.json` — Nova entrada: `etl_orchestrator` (hourly, active)
- `egos-lab/scripts/security_scan.ts` — Fix: adicionado `contrib` ao IGNORE_DIRS

**Capacidades do agente:**
```
- Monitora /api/v1/meta/etl-progress em 217.216.95.126:8000
- Detecta pipelines travados (>1h sem atualização)
- Auto-remedeia erros conhecidos (linking_hooks.py run_id)
- Registra métricas em docs/etl-orchestrator/status-log.jsonl
- Gera alertas em docs/etl-orchestrator/alerts.jsonl
```

**Para ativar 24/7:**
```bash
cd /home/enio/egos-lab
redis-server &
bash agents/worker/start-all.sh  # Inicia Worker + Scheduler
```

---

### 3. Fixes de configuração ✅

- Criado `egos-lab/packages/shared/src/social/ai-engine.ts` (stub que unblockeia o gem-hunter)
- Corrigido security_scan.ts para excluir `contrib/IPED/` (test fixtures causavam 22 false positives)

---

## ⚠️ ESTADO DO AMBIENTE — LEIA ANTES DE CONTINUAR

### IMPORTANTE: VSCode Extension vs Terminal — Diferenças Reais

```
╔══════════════════════════════════════════════════════════╗
║            COMPARAÇÃO DOS AMBIENTES                      ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║  VSCode Extension (claude.ai/code)                       ║
║  ────────────────────────────────                        ║
║  ✅ Vê arquivo aberto no editor                          ║
║  ✅ Vê seleção de texto do cursor                        ║
║  ✅ Integração nativa com diff/edição                    ║
║  ✅ Bom para: codificar, revisar, editar                 ║
║  ❌ Sem CLAUDE.md lido automaticamente no boot           ║
║  ❌ Menos controle sobre permissões                      ║
║  ❌ Session ID: 55b7d00a (esta sessão)                   ║
║                                                          ║
║  Terminal (claude no bash)                               ║
║  ──────────────────────────                              ║
║  ✅ Lê CLAUDE.md automaticamente ao entrar no diretório  ║
║  ✅ Acesso total ao filesystem                           ║
║  ✅ Pode rodar comandos interativos                       ║
║  ✅ Hooks funcionam (session-start.sh, etc.)             ║
║  ✅ /agents, /start, /end como slash commands            ║
║  ✅ Mais controle: --dangerously-skip-permissions        ║
║  ✅ Bom para: orchestration, scripts, agents 24/7        ║
║  ❌ Sem visão do arquivo aberto no editor                ║
║                                                          ║
║  MODELO: AMBOS usam claude-sonnet-4-6 (mesmo modelo)    ║
╚══════════════════════════════════════════════════════════╝
```

**Recomendação:** Use VSCode para código + use Terminal para orchestração de agentes.

---

### IMPORTANTE: Sessions no Claude Code CLI — Não São Persistentes

```
O que o terminal salva:
~/.claude/sessions/{pid}.json = { pid, sessionId, cwd, startedAt }
                                 ↑ SÓ METADADOS, SEM CONVERSA

O que NÃO é salvo:
❌ Histórico da conversa
❌ Contexto do que foi discutido
❌ Progresso de tarefas

O que PERSISTE:
✅ ~/.claude/projects/-home-enio-egos/memory/*.md  ← nossa memória
✅ CLAUDE.md em cada repo (carregado automático)
✅ agents/registry/agents.json (os agentes em si)
✅ .guarani/PREFERENCES.md, .windsurfrules (regras)
✅ docs/_current_handoffs/ (este arquivo!)
```

**Solução:** Handoffs em markdown + Memory system são o contexto persistente entre sessões.

---

### Estado dos Agentes — O Que Está Configurado

```
GLOBAL (~/.claude/)
├── manifest.json        ✅ Aponta para agents/registry/agents.json
├── commands/agents.md   ✅ /agents list, run, info, status, logs
├── hooks/
│   ├── session-start.sh ✅ Inicializa logs, verifica governance
│   ├── session-end.sh   ✅ (existe)
│   ├── agent-run.sh     ✅ Pre-flight checks para agentes
│   ├── auto-sync.sh     ✅ Sync automático
│   ├── cost-alert.sh    ✅ Alertas de custo
│   └── governance-drift-alert.sh ✅

POR REPO (egos kernel)
├── .claude/settings.json    ⚠️ Incompleto — só 2 permissões
├── .claude/config/          ✅ 4 JSONs criados
├── .claude/commands/        ✅ agents.md
├── .claude/hooks/           ✅ session-start.sh
└── CLAUDE.md                ✅ Auto-carregado no terminal

AGENTES DISPONÍVEIS
├── Kernel (egos):   6 agentes em agents/registry/agents.json
├── Lab (egos-lab): 30 agentes (incluindo novo etl_orchestrator)
└── Horleys: 1 (etl_orchestrator, AGENT-031, trigger: hourly)
```

**STATUS REAL:** Os agentes estão DEFINIDOS (registry) mas não RODANDO automaticamente.
Para rodar 24/7: precisam de Redis + Worker + Scheduler no Railway.

---

### O Que Falta para Ambiente Completo no Terminal

```bash
# 1. Verificar que CLAUDE.md está sendo lido (no terminal):
cd /home/enio/egos && claude
# → Deve mostrar contexto do EGOS kernel na primeira mensagem

# 2. Verificar /agents no terminal:
# → /agents list (deve mostrar 30+ agentes)

# 3. Configurar .guarani/ sincronizado (falta no terminal):
# .guarani/PREFERENCES.md e .windsurfrules devem ser lidos
# Verifique se .windsurfrules existe em egos:
ls /home/enio/egos/.windsurfrules  # ← precisa existir

# 4. Verificar hooks funcionando:
# ~/.claude/hooks/session-start.sh deve rodar ao iniciar
```

---

## 📋 TASKS — Estado Atual

### ✅ Completadas nesta sessão

- [x] Mapear todos os 12 repos EGOS
- [x] Criar plano completo de agenciação (3 fases, 80+ agentes)
- [x] Distribuir `.claude/` para 10 repos leaf + commits
- [x] Criar `setup-leaf-repo.sh` + `batch-setup-all-repos.sh`
- [x] **EGOS-103**: ETL Orchestrator Agent (`etl-orchestrator.ts`)
- [x] Registrar etl_orchestrator com trigger hourly
- [x] Fix security_scan.ts (contrib exclusion)
- [x] Fix packages/shared/src/social/ai-engine.ts (gem-hunter unblocked)
- [x] Criar docs/AGENT_SWARM_24_7_BLUEPRINT.md

### ⏳ Pendentes — Phase 1 P1 (Próxima Sessão)

- [ ] **AGENC-001**: Criar `agents.json` para 5 repos: 852, smartbuscas, INPI, policia, egos-self
- [ ] **AGENC-002**: Criar AGENTS.md + agents/registry/ para Santiago (delivery app)
- [ ] **AGENC-003**: Extrair PII Scanner para @egos/shared (duplicado em 4 repos)
- [ ] **AGENC-004**: Extrair Rate Limiter para @egos/shared (duplicado em 4 repos)
- [ ] **EGOS-104**: Criar agente `uptime_monitor` (monitora Supabase, Vercel, VPS, Railway)
- [ ] **EGOS-105**: Criar agente `quota_guardian` (monitora quotas Alibaba/OpenRouter/Groq)

### 🔴 Blocker — br-acc ETL (CRÍTICO)

```
Status: ETL travado em 90% desde 2026-03-06
Dados: 77M entidades carregadas, Phase 4 (post-load) falhou
Erro: linking_hooks.py ParameterMissing: run_id
Fix já aplicado localmente, falta redeploy no VPS (217.216.95.126)

Para resolver:
1. SSH no VPS: ssh 217.216.95.126
2. cd /opt/bracc
3. docker-compose restart etl
4. bracc-etl run --phase=4  # ou phase=linking
5. Verificar: curl http://localhost:8000/api/v1/meta/etl-progress
```

---

## 🚀 PRÓXIMOS PASSOS ORDENADOS

### P0 — Imediato (Fazer Primeiro no Terminal)

```bash
# 1. Entrar no terminal com contexto completo
cd /home/enio/egos
claude  # Deve ler CLAUDE.md automaticamente

# 2. Verificar o que está configurado
/agents list  # ou: claude /agents list

# 3. Verificar .windsurfrules e .guarani
cat /home/enio/egos/.windsurfrules  # Regras de governança
cat /home/enio/egos/.guarani/PREFERENCES.md  # Padrões de código

# 4. Commit dos arquivos ainda não commitados no kernel egos:
cd /home/enio/egos
git add .claude/config/ .claude/hooks/ .claude/commands/ .claude/setup-leaf-repo.sh .claude/batch-setup-all-repos.sh
git add docs/AGENT_SWARM_24_7_BLUEPRINT.md docs/CLAUDE_CODE_CLI_SETUP.md docs/_current_handoffs/
git commit -m "feat(.claude): configure kernel agent orchestration environment"
```

### P1 — Agentes dos Repos (Esta Semana)

```bash
# Criar agents.json para 5 repos que ainda não têm:
# 852 | smartbuscas | INPI | policia | egos-self

# Template base para criar rapidamente:
cd /home/enio/852 && mkdir -p agents/registry
# Criar agents/registry/agents.json com os agentes implícitos daquele repo
```

### P2 — ETL br-acc (Urgente, Mas Requer Acesso VPS)

```bash
ssh 217.216.95.126
# Redeploy do ETL com o fix do run_id já aplicado
```

---

## 🧠 RESPOSTA ÀS SUAS PERGUNTAS

### "O Claude no terminal configurou agentes — onde estão salvos?"

Os agentes que o terminal configurou estão em:
- **`~/.claude/manifest.json`** — aponta para os registros
- **`~/.claude/commands/agents.md`** — o comando `/agents` no terminal
- **`~/.claude/hooks/`** — 6 hooks de automação
- **`egos/agents/registry/agents.json`** — 6 agentes do kernel
- **`egos-lab/agents/registry/agents.json`** — 30 agentes do lab

**Eles estão disponíveis tanto no VSCode quanto no Terminal**, pois são arquivos no filesystem.

### "Sessions ficam salvas? É automático?"

**NÃO.** Cada `claude` no terminal abre uma sessão fresh.
- Só metadados são salvos: `~/.claude/sessions/{pid}.json`
- Conversa: perdida quando você fecha
- **Solução atual**: Este arquivo de handoff + memória em `~/.claude/projects/-home-enio-egos/memory/`

**Para não perder contexto:**
1. Use `/end` antes de fechar (cria este handoff)
2. Ao abrir nova sessão: leia `docs/_current_handoffs/handoff_2026-03-27.md`
3. Ou: `claude` no terminal já lerá CLAUDE.md que tem contexto básico

### "VSCode ou Terminal — qual é mais forte?"

```
Para CODIFICAR: VSCode extension (vê o arquivo aberto, seleção, melhor UX)
Para ORCHESTRAR: Terminal (CLAUDE.md auto-carregado, hooks funcionam, slash commands)
Para PRODUÇÃO 24/7: Railway Worker + Scheduler (independente de ambos)
```

**Recomendação para o seu workflow:**
- **VSCode**: código, debug, review
- **Terminal**: rodar agentes, setup, orchestração
- **Sempre dentro do egos kernel** como base (`cd /home/enio/egos && claude`)

### "O .guarani e .windsurfrules serão carregados?"

No **terminal**: O `CLAUDE.md` em `/home/enio/egos/` é carregado automaticamente. Se `CLAUDE.md` referenciar `.guarani/PREFERENCES.md` e `.windsurfrules`, Claude os lê também.

**Verifique se isso está configurado:**
```bash
cat /home/enio/egos/CLAUDE.md | grep -E "guarani|windsurfrules|preferences"
```

Se não estiver referenciado, adicione ao CLAUDE.md:
```markdown
## Governance
- Read `.guarani/PREFERENCES.md` for coding standards
- Read `.windsurfrules` for context-specific rules
```

---

## 📁 ARQUIVOS CRIADOS ESTA SESSÃO

### egos kernel (não commitados ainda):
- `.claude/config/mcp-servers.json` — Config MCP servers
- `.claude/config/permissions.json` — RBAC gates
- `.claude/config/memory.json` — Sistema de memória híbrido
- `.claude/config/output-styles.json` — Estilos de output
- `.claude/setup-leaf-repo.sh` — Bootstrap para repos leaf
- `.claude/batch-setup-all-repos.sh` — Setup em lote (todos os 10 repos)
- `.claude/hooks/session-start.sh` — Hook de inicialização
- `.claude/commands/agents.md` — Comando /agents
- `docs/AGENT_SWARM_24_7_BLUEPRINT.md` — Blueprint mestre
- `docs/CLAUDE_CODE_CLI_SETUP.md` — Guia de setup
- `docs/_current_handoffs/handoff_2026-03-27.md` ← este arquivo

### egos-lab (commitados em `2db4154`):
- `agents/agents/etl-orchestrator.ts` ✅
- `agents/registry/agents.json` (atualizado com etl_orchestrator) ✅
- `test-etl-orchestrator.ts` ✅
- `scripts/security_scan.ts` (fix IGNORE_DIRS) ✅
- `packages/shared/src/social/ai-engine.ts` (stub) ✅

---

## 📊 SESSION SUMMARY
```
═══════════════════════════════════════════════════════
SESSION SUMMARY — 2026-03-27
═══════════════════════════════════════════════════════
Repo:         egos (kernel) + egos-lab
Commits:      1 (egos-lab: 2db4154)
Security:     Clean (security scan fix included)
Files changed: 10 criados + 5 modificados

What was done:
  1. Distribuiu .claude/ para 10 repos leaf (todos commitados)
  2. Criou ETL Orchestrator Agent EGOS-103 (340 linhas, 4 testes)
  3. Registrou etl_orchestrator com trigger horário no scheduler
  4. Corrigiu security_scan (false positives em IPED test fixtures)
  5. Criou social/ai-engine.ts stub (desbloqueia gem-hunter)

Next steps P0:
  1. Commit arquivos .claude/ no kernel (egos)
  2. Verificar ambiente no terminal (CLAUDE.md, .windsurfrules)
  3. Criar agents.json para 5 repos (852, smartbuscas, INPI, policia, egos-self)
  4. Redeploy ETL br-acc via SSH no VPS

Context Tracker: ~180/280
Signed by: claude-sonnet-4-6 — 2026-03-27T12:45:00Z
═══════════════════════════════════════════════════════
```

---

## 🔗 REFERÊNCIAS ESSENCIAIS PARA PRÓXIMA SESSÃO

```
~/.claude/projects/-home-enio-egos/memory/AGENCIACION_PLAN_2026.md  ← Mapa completo
~/.claude/projects/-home-enio-egos/memory/CLAUDE_CODE_CONFIG_STATUS.md ← Status config
/home/enio/egos/TASKS.md  ← SSOT de tasks (EGOS-103 já marcado completo)
/home/enio/egos/docs/AGENT_SWARM_24_7_BLUEPRINT.md  ← Blueprint 24/7
/home/enio/egos-lab/agents/registry/agents.json  ← 30 agentes registrados
/home/enio/egos-lab/agents/worker/start-all.sh  ← Para ativar 24/7
```
