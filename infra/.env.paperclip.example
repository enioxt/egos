# infra/.env.paperclip.example — Copy to VPS at /opt/apps/paperclip/.env
# NEVER commit the real .env — only this example file

PAPERCLIP_DB_PASSWORD=change-me-strong-password
BETTER_AUTH_SECRET=generate-with-openssl-rand-hex-32
PAPERCLIP_PUBLIC_URL=https://paperclip.egos.ia.br
