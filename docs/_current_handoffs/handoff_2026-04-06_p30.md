# Handoff P30 — 2026-04-06

## Accomplished

- **Codex Proxy** ([`~/.openclaw-codex-proxy/proxy.js`](~/.openclaw-codex-proxy/proxy.js)): Node.js HTTP server on port 18802 wrapping `codex exec` as OpenAI-compatible API. Quota tracking (5h window, 10 req max), HTTP 429 on exhaustion, usage persisted to `usage.json`.
- **OpenClaw Codex Provider**: Added `codex` provider to models.json + auth-profiles.json. OpenClaw can now route to `gpt-5.4` via local proxy.
- **Constitutional Review Job** ([`~/.openclaw/jobs/constitutional-review.sh`](~/.openclaw/jobs/constitutional-review.sh)): Codex reviews last 5 commits against 8 constitutional rules, signs with SHA-256, posts to Supabase. Runs 06:00 + 18:00 via cron.
- **VPS Codex Proxy**: Codex v0.118.0 installed on VPS, proxy synced, UFW rule for Docker bridge (172.19.0.0/16:18802), `@reboot` cron.
- **HQ v2 Collapsible Dashboard** ([`apps/egos-hq/app/page.tsx`](apps/egos-hq/app/page.tsx)): 5 services (guard_brasil, gateway, openclaw, billing_proxy, codex_proxy), collapsible cards, quota bars, action buttons (▶ Rodar Review / ↻ Refresh token), toast notifications.
- **HQ Action Endpoints**: `POST /api/hq/actions/codex-review`, `POST /api/hq/actions/billing-refresh`, both added to middleware PUBLIC_PATHS.
- **Smart TASKS Archiving** ([`scripts/archive-tasks.sh`](scripts/archive-tasks.sh)): Pre-commit auto-archives completed tasks at 490+ lines. 74 tasks archived this session (510→435 lines). TASKS_ARCHIVE.md created.
- **Pre-commit TTY fix** ([`.husky/pre-commit`](.husky/pre-commit)): `[ -e /dev/tty ]` → `[ -t 0 ]` — correctly detects interactive context.
- **GTM SSOT** ([`docs/GTM_SSOT.md`](docs/GTM_SSOT.md)): 7 scattered GTM files consolidated. SSOT-First rule in `.guarani/orchestration/SSOT_RULES.md` + `CLAUDE.md §26`.
- **HARVEST.md v3.8**: Codex CLI wrapper pattern, proxy bind 0.0.0.0, TASKS smart archive, HQ collapsible, constitutional reviewer patterns.
- **CAPABILITY_REGISTRY.md §16**: 6 new capabilities documented.
- **All 5/5 services green**: guard_brasil (20ms), gateway (14ms), openclaw (14ms), billing_proxy (12ms), codex_proxy (12ms).

## In Progress

- **HQ rebuild on VPS**: Container rebuilt and running but may need re-verify after env var changes.
- **Constitutional review cron on VPS** (OC-033): Script copied, cron NOT yet set on VPS.

## Blocked

| ID | Blocker | Action needed |
|----|---------|---------------|
| OC-031 | Codex auth.json expires (last_refresh: 2026-04-01) | Implement refresh cron on VPS — `codex auth refresh` or token exchange |
| OC-033 | Constitutional review cron missing on VPS | SSH and add `0 6,18 * * * bash /root/.openclaw/jobs/constitutional-review.sh` |
| OC-034 | No Telegram alert when Codex quota ≥80% | Wire quota check to Telegram bot |
| OC-021 | WhatsApp multi-device QR | Requires phone present |
| M-007 | 5 DPO outreach emails | **7+ days stale — #1 revenue blocker** |

## Next Steps (priority order)

1. **M-007**: Send 5 DPO outreach emails (templates in `docs/GTM_SSOT.md §5`) — every day this isn't done is R$0 revenue
2. **OC-033**: Add constitutional review cron on VPS (`ssh hetzner` → `crontab -e`)
3. **OC-031**: Implement Codex token auto-refresh on VPS before token expires
4. **OC-034**: Quota alert → Telegram when ≥80%
5. **GH-067**: Deploy gem-hunter-server to VPS (gemhunter.egos.ia.br) — next revenue vector
6. **GTM-002**: Post X.com thread (5 tweets ready in GTM_SSOT.md §4)

## Environment State

- All 5 services: GREEN (guard_brasil, gateway, openclaw, billing_proxy, codex_proxy)
- TASKS.md: 435 lines (was 510, 74 archived)
- HQ: hq.egos.ia.br — live with collapsible dashboard v2
- Codex proxy: local (port 18802) + VPS (port 18802, 0.0.0.0)
- Session commits: ~22 commits in P28-P30 window
